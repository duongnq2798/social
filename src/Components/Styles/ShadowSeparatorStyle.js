import {StyleSheet} from 'react-native';
import Metrics from '../../Themes/Metrics';

const styles = StyleSheet.create({
  container: {
    paddingVertical: Metrics.distant.pad10,
  },
});

export default styles;

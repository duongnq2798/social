import {
  scale,
  verticalScale,
  moderateScale as ms,
} from 'react-native-size-matters';

const vScale = verticalScale;
const wScale = ms;
const moderateScale = ms;

export {vScale, wScale, moderateScale, scale};
